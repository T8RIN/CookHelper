<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.CookHelper" parent="Theme.Material3.DayNight.NoActionBar" />

    <style name="Theme.CookHelper.Launcher" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.CookHelper</item>
    </style>

</resources>